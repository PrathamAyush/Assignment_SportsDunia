export const setArticles = (articles) => ({
    type: 'SET_ARTICLES',
    payload: articles,
  });
  
  export const setPayout = (payout) => ({
    type: 'SET_PAYOUT',
    payload: payout,
  });
  
  export const setFilters = (filters) => ({
    type: 'SET_FILTERS',
    payload: filters,
  });
  